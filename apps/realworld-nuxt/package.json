{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxi dev --dotenv .env.development",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "nx": {
    "build": {
      "outputs": ["{projectRoot}/.output", "{projectRoot}/.nuxt"]
    },
    "tags": [
      "scope:realworld-nuxt"
    ],
    "implicitDependencies": [
      "tag:scope:realworld-nuxt"
    ]
  }
}
